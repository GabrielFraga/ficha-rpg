// import { call, put, select, all, takeLatest } from 'redux-saga/effects';

// import { editHabilitySuccess } from './actions';

// function* editHabilityMiddlewere(name, value, mod) {
//   yield put(editHabilitySuccess(name, value, mod));
// }

// export default all([
//   takeLatest('@hability/EDIT_REQUEST', editHabilityMiddlewere),
// ]);
